query {
    survey(survey: state_of_css) {
        currently_missing_from_css_matchups: bracket_matchups(id: opinions__currently_missing_from_css, filters: {}, options: {}) {
            id
            year(year: 2021) {
                year
                completion {
                    total
                    count
                    percentage_survey
                }
                facets {
                    completion {
                        total
                        count
                        percentage_survey
                        percentage_question
                    }
                    buckets {
                        id
                        matchups {
                            id
                            count
                            percentage
                        }
                    }
                }
            }
        }
    }
}
