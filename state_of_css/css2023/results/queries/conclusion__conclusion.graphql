query {
    entities(ids: ["lea_verou"]) {
        id
        name
        homepage{
            name
            url
        }
        twitterName
        twitter {
            name
            url
        }
    }
}
