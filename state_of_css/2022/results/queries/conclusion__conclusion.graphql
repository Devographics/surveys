internalAPI {
    entities(ids: ["lea_verou"]) {
        id
        name
        homepage{
            name
            url
        }
        twitterName
        twitter {
            avatarUrl
            userName
        }
    }
}
