query {
    survey(survey: state_of_js) {
        # fragmentStart
        build_tools_experience_linechart: tools_rankings(ids: [webpack,parcel,gulp,rollup,browserify,tsc,rome,snowpack,swc,esbuild,vite,wmr,turbopack]) {
            ids
            years
            experience(filters: {}) {
                id
                entity {
                    name
                    nameClean
                    nameHtml
                }
                usage {
                    year
                    rank
                    percentage_question
                }
                awareness {
                    year
                    rank
                    percentage_question
                }
                interest {
                    year
                    rank
                    percentage_question
                }
                satisfaction {
                    year
                    rank
                    percentage_question
                }
            }
        }
        # fragmentEnd
    }
}
