query {
    survey(survey: state_of_js) {
        # fragmentStart
        testing_experience_linechart: tools_rankings(ids: [jest,mocha,storybook,cypress,ava,jasmine,puppeteer,testing_library,playwright,webdriverio,vitest,selenium,testcafe]) {
            ids
            years
            experience(filters: {}) {
                id
                entity {
                    name
                    nameClean
                    nameHtml
                }
                usage {
                    year
                    rank
                    percentage_question
                }
                awareness {
                    year
                    rank
                    percentage_question
                }
                interest {
                    year
                    rank
                    percentage_question
                }
                satisfaction {
                    year
                    rank
                    percentage_question
                }
            }
        }
        # fragmentEnd
    }
}
