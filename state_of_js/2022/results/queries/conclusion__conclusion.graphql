query {
    entities(ids: ["sarah_drasner"]) {
        id
        name
        homepage{
            name
            url
        }
        twitterName
        twitter {
            avatarUrl
            userName
        }
    }
}
