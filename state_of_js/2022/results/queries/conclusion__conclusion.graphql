query {
    entities(ids: ["shawn_wang"]) {
        id
        name
        homepage{
            name
            url
        }
        twitterName
        twitter {
            avatarUrl
            userName
        }
    }
}
